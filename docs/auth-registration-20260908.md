# 邮箱注册与账号绑定：2026-09-08

本轮完成：登录页区分邮箱注册/已有账号登录；验证码验证后才返回账户是否存在；已有会话可绑定尚未使用的邮箱。删除本地环境按“唯一无邮箱用户”自动归并身份的逻辑。设备、角色、权益始终保留原 user_id。旧接口省略 intent 仍支持自动注册，但不再合并旧账户。

验证：邮箱与 onboarding 回归共 13 项通过；绑定写入条件更新后复跑 11 项邮箱测试通过；Next.js 生产构建通过。无需数据库迁移。

本地 development 邮件模式只显示测试验证码，不是真实邮件。正式发信须配置 SMTP 并实测。旧无邮箱账户必须先登录原账户，在账户页面绑定；不能通过新注册取得旧设备。

后续未完成：密码登录/重置、手机短信供应商接入、微信浏览器绑定与回调加固、按已配置能力显示入口、海外服务验收。未配置服务不添加假入口。本轮不声称多种登录方式已全部交付。

回退：无数据库结构变化；可回退界面，但不要恢复未经身份验证的自动账户归并。服务重启前配置和数据库由 start_local_pilot.ps1 自动备份于 run/backups。
