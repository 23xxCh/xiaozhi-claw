# 第三批静默验收：网页注册与配置

日期：2026-09-09。业务代码未修改；新增一个浏览器回归用例及隔离执行脚本。

## 结果

- 隔离 Next.js 生产构建成功，Playwright **27 passed、0 skipped、0 failed**，最终运行约22.5秒。
- 新用例经真实 FastAPI 和临时 SQLite 完成邮箱注册、四项使用确认、进入角色页、修改语音方案与音色、保存及刷新。核对数据库返回的角色配置版本增长。
- 不兼容方案/音色 PATCH 返回422；另一测试账户修改该角色返回404；两次拒绝后原配置和版本不变。
- 原有26条回归也通过，涵盖配网引导、认领页面、解绑确认、角色选择、导航及可访问性。其中设备相关页面使用接口模拟，不能当成真实设备认领、解绑或配网验收。
- 8个控制台页面 × 5种宽度无横向溢出，生产构建下该用例约6.0秒。最初开发构建因40次导航中的冷编译超过默认30秒；未提高超时或放宽布局断言。
- 新测试第一次直接请求未带 Origin，按现有Cookie来源校验返回403；修正测试请求后通过，未放宽服务端安全规则。
- 新Python脚本Ruff、新浏览器用例ESLint通过。生产构建包含TypeScript检查。

最终本地报告：`run/web-e2e-i_uy8r80/playwright-report.json`；构建和服务日志在同目录。此目录被Git忽略。

## 重复执行

在已安装项目Python、网页依赖和Playwright Chromium的环境中，从仓库根目录执行：

```powershell
.venv/Scripts/python.exe scripts/web_e2e_smoke.py
```

脚本复用已有隔离环境和进程清理工具，每次创建独立目录、数据库、随机本机端口和网页生产构建。只复制明确列出的网页源码与配置，不复制.env或日常数据库；供应商使用mock。测试结束或异常时只停止本次创建的进程。

依赖目录通过junction/symlink复用，保留报告和构建供定位；不要对链接目标执行递归删除。新增注册用例仅在脚本设置的隔离标志下运行，并拒绝默认3000端口。一般浏览器测试命令会跳过该用例；不要手工对日常服务设置隔离标志。

## 尚未验收

邮箱使用development测试验证码，未测试SMTP真实投递。没有测试硬件音频、真实供应商、目标MySQL、容量或恢复；没有更改账户真实权益、角色或设备，没有重启3000/8000/8001日常服务，也没有部署或烧录。

第三批仍有迁移、并发赠送/保存、真实双进程解绑与删除记忆等验收项；本记录只关闭上述网页检查，不表示生产放行。此次仅增加开发检查，回退无需数据库迁移。
