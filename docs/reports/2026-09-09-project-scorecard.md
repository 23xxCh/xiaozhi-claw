# Hensun 当前工程评分

日期：2026-09-09。依据当前本地工作树、核心模块抽查、静态检查、只读数据库检查及本次连续真机排障记录。不是逐行全仓审计、渗透测试或生产验收。分数是工程判断，误差约 ±5 分；不能用平均分绕过关键故障。

| 维度 | 分数 / 100 | 权重 | 依据与扣分 |
|---|---:|---:|---|
| 代码质量 | 72 | 10% | 有类型、测试与 CI；当前后端 Ruff 21 项未通过（18 行长、3 导入顺序），包括本轮开发期间新增代码 |
| 可维护性 | 65 | 15% | 有对话协议、供应商适配器、ADR；实时 session.py 2687 行，仍集中处理多种职责，修改风险偏高 |
| 前端 | 76 | 10% | Next.js/React/TypeScript，统一 API 错误与音色兼容筛选；本次类型和 ESLint 通过。浏览器交互、移动端与可访问性本次未重新验收 |
| 后端 | 68 | 15% | FastAPI 控制面/实时网关分开，具备轮次、取消、播放确认；连接建立、收音判停和错误恢复还需稳定验证 |
| 数据库 | 72 | 10% | SQLAlchemy、12 个迁移文件、外键/索引/唯一约束及 MySQL CI；本地 SQLite quick_check=ok，外键违规=0，迁移到 20260909_11。并发与备份恢复未在本次验证 |
| 用户体验 | 50 | 15% | 注册、认领、角色与语音可用；近期存在唤醒后不回复、自动休眠、服务错误，仍依赖研发排障 |
| 固件与音频 | 58 | 10% | 有板型边界、播放确认、VAD 和缓冲控制；最新固件 167 项主机检查通过（1 跳过）、编译烧录成功，但多轮和声音体验未充分验证 |
| 运行与发布 | 45 | 10% | 服务端 Compose 有 restart 和 healthcheck，已有部署资料；本地两个服务近期退出，需要人工恢复，原因未明。不能据此认定远端部署也相同 |
| 安全与隐私准备 | 65 | 5% | 有权限、设备身份、记忆授权/加密/版本失效；ADR 明确记录备份删除重放、密钥轮换、记忆配额/TTL 等未完成事项；无本次渗透测试证据 |

加权分 63.25，汇报取整为 **63/100**。定位：可继续工程内测的原型，尚不足以按稳定零售产品交付。

## 本次检查

- `python -m ruff check backend --statistics`：21 项，未通过；格式问题不等同于 21 个运行故障。
- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `node node_modules/eslint/bin/eslint.js .`（web）：通过。
- SQLite 以只读方式检查：quick_check=ok；foreign_key_check=0；journal_mode=delete。没有进行配置修改。
- 读取 `.github/workflows/quality.yml`：存在 MySQL 迁移往返、前端构建和 Playwright/Axe 等作业；本次没有证明远端 CI 全绿。
- 读取 `docs/adr/0008-memory-lifecycle.md`：明确保留未完成项，不将设计文本视为实现完成。
- 本次未重跑全部后端测试、浏览器 E2E、负载与长时间真机测试；此前 24 项相关后端回归和 167 项固件主机检查只覆盖其各自范围。

## 优先顺序

1. 查清进程退出，完成故障恢复验证；复现并关闭唤醒/连续对话故障，记录失败分母与延迟分布。
2. 通过同一候选版本的完整 CI、100 轮对话与持续运行检查，再改善错误提示和重新聆听体验。
3. 在回归保护下按会话生命周期、输入与播放职责逐步缩小实时主模块；保留 FastAPI 和现有适配器。
4. 在隔离生产候选环境验证 MySQL 并发、备份恢复和记忆删除语义，再决定试用放量。

架构结论：现有技术栈可以继续使用；当前证据不支持为了评分全面重写或增加微服务。可靠性和验收优先于新增模型路线。
