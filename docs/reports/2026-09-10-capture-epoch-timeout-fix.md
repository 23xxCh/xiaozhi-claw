# 采集代次与首包等待修复

在输入收尾修复 09bea8f 的基础上，处理排队 VAD/停止事件跨轮，以及托管、端到端语音等待无结果的问题。

## 行为

- AudioService 开始新采集、停止和正常收尾时更新采集代次；AFE 输出与停止已由上一项修复互斥。
- VAD 回调携带产生时的代次，主任务执行时再次校验。旧开始/结束事件不能改变新一轮的说话标志，也不能清除新一轮等待处理的 VAD 标志。
- 停止事件记录请求时的代次，下一轮不会执行旧停止请求。
- 托管/端到端共用 S2S 处理从开始收尾起，20 秒内必须向设备成功发送首个音频包。持续的状态、文字或用量事件不延长期限；首次发送后进入 120 秒回复/播放收尾期限。
- 固件等待回复的最后保护改为 30 秒，给正常网关错误反馈留出时间。网关错误和本地等待超时均增加固定的中文屏幕通知，提示重新唤醒重试，不显示供应商内部异常。
- 没有增加跨供应商自动切换，也没有将错误当作成功或免费用量。

## 软件证据

- S2S 与授权检查：23 passed。新增无事件、持续事件无音频的绝对超时检查，验证错误码、上游取消、解码清理、不写成功历史及失败计量；验证首次音频后不会仍受初始期限误杀。
- 网关、阿里适配器、豆包适配器、实时媒体回归：72 passed。
- 固件相关检查及 C++ 主机动态测试：79 passed。动态执行生产 VAD 处理及停止方法，覆盖旧开始、旧结束、旧停止事件与新轮次，以及新轮正常结束。
- Ruff 与 diff whitespace 检查通过；后端测试存在既有 Starlette/httpx 弃用告警。
- ESP-IDF 6.0.2 NOCAM local 构建通过，应用 2793072 字节，SHA256 `bd411bb74b5e8a929e7390e043b98d8c1a944169c4ed45e5a8ae72ffaad6f414`。

## 验收边界

## 本地部署与设备证据

- 修复提交：785ec0e，包含此前 09bea8f 输入收尾修复；本地网关已重启加载。
- COM6 read-mac 确认当前设备为 `7c:4f:ad:2b:51:a8`。烧录前读取完整 ota_0 应用分区到 `run/backups/20260910-input-epoch/ota0-before.bin`（4128768 字节），SHA256 `aa0ba70537dad43b02e756a82a93952f9d45822d9a3b8646fc4a4532870decd8`。
- 仅向 0x20000 写入上述候选 xiaozhi.bin，esptool 返回 Hash of data verified；没有改写 NVS、密钥或资源分区。
- 联调发现原控制服务 PID 34828 已不存在，8000 没有监听，日志没有退出原因。已恢复本地控制服务，尚未完成异常退出根因与自动恢复修复。
- 控制面和网关 `/health/ready` 均返回 200；新网关日志确认设备 192.168.1.21 的 WebSocket accepted/connection open。串口观察到麦克风采样日志，但这不证明人声识别或回复已经验收。
- 这是本地开发环境更新，不是湖北云端部署，未推送 GitHub。

## 剩余验收

主机测试使用硬件替身，不替代实际 AFE、麦克风、网络及扬声器。20/30 秒是失败保护上限，不是正常回复延迟目标。本次首包期限作用于 S2S 共用路径，串联供应商各阶段期限没有整体重写。

尚须真机确认新提示可见、唤醒/BOOT/连续追问、短句及停止场景。一次自动重新聆听窗口、迟到网络事件的全类型隔离与取消来源完整追踪仍属后续工作。不能据此宣称所有不回复问题已解决或七条路线已验收。
