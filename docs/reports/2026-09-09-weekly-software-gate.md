# 本周交付：软件基线第一批

状态：软件基线通过，**全路线交付未完成**。执行清单见
`docs/plans/2026-09-13-weekly-delivery.md`。

## 本批变更与核验

- 修复21项Ruff问题；四个运行模块修改前后AST相同，没有改变语音运行逻辑。
- 补齐两个供应商放行测试中的ASR字段，继续测试凭据、生产验证及HTTPS/WSS限制。
- 重新生成OpenAPI，唯一新增接口为已有 `/v1/voice-library` 的文档描述。
- 完整后端及工具测试：424通过、1跳过。跳过的真实Qwen集成仍需独立调用验收。
- `ruff check backend migrations scripts`：全部通过。
- 当前网页浏览器测试：26通过、1隔离注册测试跳过。
- 随后运行既有隔离生产构建/浏览器脚本：27通过、0跳过、0重试通过。
  实际临时目录 `run/web-e2e-nzn76i10`，使用独立数据库和mock模型。
- 双进程WSS检查：通过；一轮mock音频、ready/drained、解绑关闭4403、Outbox delivered。
  该脚本使用metadata-create-all，不替代MySQL迁移往返。

## 云端情况

注册SSH别名 `codex-hubei-root` 直接探测连接被远端关闭；技能预检同样无法连接。
本批没有上传发布包、修改云端数据或重启远端服务。
测试域名 `/health/ready` 返回HTTP 200，只证明原部署就绪，不能当成当前提交已部署。

## 尚未完成

七路线权限/真机100轮及各2小时压力、8小时待机、收音修复复验、有限错误恢复、
版本化验收标识、MySQL迁移和恢复、记忆删除恢复语义、云端发布和故障注入。
本批不改变生产开关、默认角色、音色，也不提高项目评分。
