# 语音稳定性回归与架构处理结果（2026-09-08）

本轮保留 Next.js、FastAPI 双进程和 ESP-IDF，不进行全面重构。先运行现有回归与双进程联调，只修复可复现问题。

## 已修复

1. Qwen 情绪 TTS 重连取消竞态：取消发生在新 WebSocket 建连期间，旧实现仍接受迟到连接并发送配置。新增确定性回归（延迟连接直到取消结束再返回），修复前失败，修复后通过。现在先标记关闭，迟到连接立即关闭；取消后拒绝情绪更新、合成和队列中的旧音频。
2. OpenAPI 契约陈旧：通过 scripts/export_openapi.py 重新生成，补齐既有邮箱绑定、onboarding device_id、managed_app 与能力/用量字段。不改变接口实现。

## 验证

- 全量 backend/tests 回归发现 1 个失败：旧 OpenAPI 与当前服务不一致；1 个真实 Qwen 集成测试默认跳过，其他测试通过。本轮没有把该跳过项记为通过。
- 修复后受影响的提供商、取消、回退、S2S 权限、播放握手回归：83 passed。
- 重新生成契约后 gateway_contract：27 passed。
- Ruff 通过；Next.js 生产构建和 TypeScript 检查通过。
- scripts/dual_process_smoke.py 通过：2 个独立进程、WSS、1 轮 mock 音频、ready/drained 确认、Outbox delivered，撤权断连代码 4403。此测试使用隔离数据库，不是物理扬声器验收。
- 本地服务已重新加载修复，8000/8001 健康检查通过。未改角色选择，未烧录固件，未推送代码。

## 真机证据与下一道门槛

用户反馈“有反应，继续”，仅记录设备可响应，不推断指定路线、连续三轮、无截尾或停止有效。

各语音路线仍需独立采集两组各 50 次、每组至少 5 个会话；使用已有 scripts/voice_stability_acceptance.py 评估成功率与 speaker_pcm_started 延迟。不可混合多条路线或重复日志来凑足样本。

还需真机实听：短句尾字、长句停顿、说话中停止、断网恢复、表情退出；随后进行 2 小时连续对话、8 小时待机和容量验证。本轮没有达到商业放行条件。

架构结论：本次暴露的是会话生命周期与接口契约维护问题，通过共享实现局部修复即可解决；尚无证据要求换框架、拆微服务或整体重构。

回退：保留用户数据库，以代码版本回退为主；不要用旧数据库覆盖新增数据。启动前备份为 run/backups/20260908-230500/hensun-lan.db。
