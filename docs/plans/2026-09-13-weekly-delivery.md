# 本周交付执行单

用户已确认：2026-09-13 前交付当前 NOCAM 一台云端内测样机；湖北服务器，
staging.hensun-desk.top；每天约 1–2 小时真人验收。保留 FastAPI、Next.js、
MySQL 与适配器。不发布正式域名，不新增硬件、供应商、支付或自然打断。

## 必验路线

| 预设 | 路线 | 真人两组各50轮 | 两小时压力 | 当前结论 |
|---|---|---|---|---|
| fast-chat | 阿里ASR / DeepSeek Flash / 阿里TTS | 待完成 | 待完成 | 未放行 |
| rich-chat | 阿里ASR / DeepSeek Pro / 阿里TTS | 待完成 | 待完成 | 未放行 |
| expressive-chat | 阿里ASR / DeepSeek / 情绪TTS | 待完成 | 待完成 | 未放行 |
| volc-tts-chat | 阿里ASR / DeepSeek / 火山TTS | 待完成 | 待完成 | 未放行 |
| volc-chat | 火山ASR / DeepSeek / 火山TTS | 待完成 | 待完成 | 未放行 |
| aliyun-dialog | 阿里托管应用 | 待完成 | 待完成 | 未放行 |
| doubao-realtime | 豆包端到端 | 待完成 | 待完成 | 未放行 |

stable-fallback 不成为用户入口，仅测既有恢复契约，不新增跨路线自动切换。
目录启用不等于完成验收。阿里应用按真实能力验收，不承诺支持项目记忆或两个
可选音色；单一应用音色例外须明确记录，不能伪造第二个音色。

## 顺序和截止

- 9/10：固化基线、关闭唤醒/多轮阻断、确认七路线权限和云端访问。
- 9/11：全套软件检查、网页闭环、首次云端候选部署；电脑关闭后可用。
- 9/12：七路线口测、故障注入、备份恢复；晚间固定候选版本长稳。
- 9/13：修复复测、固定发布提交和固件哈希、交付使用与回退说明、重新评分。

## 硬性验收

每条路线每组至少5个会话、50轮、成功率≥98%；软件首响P50≤2000ms、
P90≤2500ms；工具查询单独统计。抽样记录实际停说到听见声音的延迟。
覆盖唤醒、BOOT、短句、长句、追问、停止、断网、角色/音色切换，禁止截尾、
串轮、读情绪词与卡表情。每路线至少默认及第二兼容音色（单音色路线记录例外）。
七路线各两小时压力、整机八小时待机；模拟与真人数据分开，版本改变重验受影响项。

完整后端/工具测试、Ruff、前端类型/ESLint/生产构建、浏览器E2E、MySQL迁移、
固件检查必须通过。隔离负载按5/20/50连接阶梯；真实供应商并发受控。

## 运行与数据

核实版本化部署、HTTPS/WSS、内部应用端口、进程恢复及健康告警。
迁移前备份；云端原归属有效则保留，否则通过云端账号重新认领，不覆盖数据库。
验证隔离恢复、记忆权限/删除和旧任务失效；不能保持删除语义则不开放记忆。
未知成本保持未知。先后端/网页、再固件、最后开放通过的路线。
回退应用保留新数据；不使用旧数据库覆盖新用户数据。

## 当前总状态

全路线交付未完成。软件检查、云端部署、真人与长稳证据分别记录；不以评分代替门禁。
允许交付工程候选，但任何路线未通过都不能报告全路线完成。
